import { EmptyState } from "@/components/EmptyState";

export const NotAvailable = () => (
  <EmptyState message="NOT YET AVAILABLE" description="This data is published once the session has run." />
);
