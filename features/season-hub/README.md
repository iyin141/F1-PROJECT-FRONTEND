# Season Hub Feature

Purpose: UI for season calendar and standings. Implement views inside `components/` and exports via `index.ts`.

Local structure:

- `components/`: UI pieces
- `api.ts`: data access helpers
- `types.ts`: local types

Status: scaffolded for developer implementation.
