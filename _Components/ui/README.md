# Shared UI primitives

Place small, theme-agnostic UI components here. Keep props minimal and avoid feature-specific logic.

Examples: `Button.tsx`, `Icon.tsx`, `Card.tsx`.

Export components from index files when composing shared libraries.
